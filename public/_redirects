# Cloudflare Pages _redirects file for EatPal
# https://developers.cloudflare.com/pages/platform/redirects/

# Common authentication redirects
/signup /auth?tab=signup 302
/signin /auth?tab=signin 302
/login /auth?tab=signin 302
/register /auth?tab=signup 302

# Help and support redirects
/help /contact 301
/support /contact 301

# Social media redirects
/instagram https://instagram.com/eatpal 302
/facebook https://facebook.com/eatpal 302
/twitter https://twitter.com/eatpal 302

# Admin panel redirect if not authenticated
/admin /auth?redirect=/admin 302

# Handle trailing slashes consistently
/kids/ /kids 301
/planner/ /planner 301
/tracker/ /tracker 301
/recipes/ /recipes 301
/pantry/ /pantry 301
/grocery/ /grocery 301
/dashboard/ /dashboard 301

# public/llms.txt advertised /features/kids-meal-planning and /solutions/arfid-meal-planning
# to every AI crawler for months. Neither was ever a route, so both served the SPA 404.
# The llms.txt entries are gone now; these 301s catch anything that cached them.
/features/kids-meal-planning /guides 301
/features/* /guides 301

# US-649: /solutions/* is a real route now, so the catch-all that sent the whole
# namespace to /guides is gone. Only the one URL llms.txt actually advertised keeps a
# redirect, and it points at the page that was finally built for it. Anything else under
# /solutions/ falls through to the SPA, where an unknown slug renders the 404 page --
# which is the honest answer, unlike a 301 to an index that does not answer the query.
/solutions/arfid-meal-planning /solutions/arfid 301

# Legacy pSEO URLs -> the /guides/ mount point.
# pseo_pages.slug already carries its own prefix ('food-chaining/chicken-nuggets') and
# the generator writes canonical_url as /guides/<slug>, so /guides is the canonical
# surface. These 301s make sure any link or index entry pointing at the old
# un-namespaced paths lands on the URL the page declares as canonical, instead of the
# SPA 404 those paths served before.
/food-chaining/* /guides/food-chaining/:splat 301
/challenges/* /guides/challenges/:splat 301
/age/* /guides/age/:splat 301
/meals/* /guides/meals/:splat 301
/dietary/* /guides/dietary/:splat 301
/food-challenge/* /guides/food-challenge/:splat 301

# Note: CloudFlare Pages automatically handles SPA fallback
# Static files are served first, then index.html for all other routes
# No explicit /* /index.html 200 rule needed (causes infinite loop warning)

