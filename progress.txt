# Ralph Progress Log — Web Hardening v3

## Codebase Patterns
- `npm run typecheck` = `tsc -b --noEmit` (plain `tsc --noEmit` is a no-op: root tsconfig is files:[]+references). Always use the script.
- Quality gate before commit: `npm run lint && npm run typecheck && npm run test:run`. There is a large PRE-EXISTING type/lint backlog (~1100 errors, tracked by US-342); do not let it block — only ensure YOUR story adds no new errors. CI treats lint+typecheck as non-blocking annotations.
- Web app: Vite 7 + React 19 + TS. State in src/contexts/AppContext.tsx (+ domain contexts Grocery/Plan/Kids/Recipes). DB snake_case, UI camelCase (normalizeRecipeFromDB).
- This branch is scoped to WEB stories only (build/typecheck/test verifiable here). Do NOT touch ios/ Swift or attempt prod deploys.

---
