# tasks/prd-agentic-os.md, archived 2026-09-18 (US-788)

Converted to `prd-agentic-os.json` at the repo root and archived here so there
is one source of truth rather than two.

The markdown held 41 stories (US-475..US-515) that no tool in this repo read:
the loop, the verify workflow and every PRD script look for `prd*.json`. It
read as a backlog of work to do while being a record of work already done --
all 41 artefacts it describes exist, which is exactly what made it a shadow
backlog rather than a plan.

Kept rather than deleted because it carries the prose the JSON does not: the
tier model, the architecture diagram, the non-goals, the success metrics and
the open questions. The JSON carries the stories and, per story, the artefact
that proves it.
