package com.eatpal.app.ui.theme

import androidx.compose.ui.unit.dp

/**
 * Spacing tokens mirroring iOS `AppTheme.Spacing`. Keep in sync — UI parity
 * across platforms depends on these values matching.
 */
object Spacing {
    val xs = 4.dp
    val sm = 8.dp
    val md = 12.dp
    val lg = 16.dp
    val xl = 24.dp
    val xxl = 32.dp
}
