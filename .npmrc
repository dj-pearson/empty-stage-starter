# Allow installing packages even when peer dependency conflicts exist in CI
# This is a temporary/workaround to avoid build failures on Cloudflare Pages
# See TODO: audit and fix upstream dependency mismatches (e.g. @react-three/drei vs @react-three/fiber)
legacy-peer-deps=true

